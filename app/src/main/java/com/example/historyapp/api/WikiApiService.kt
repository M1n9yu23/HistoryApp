package com.example.historyapp.api
/*
import com.example.historyapp.model.WikiResponse
import retrofit2.http.GET
import retrofit2.http.Query

*/
/**
 * Wikipedia API 호출을 위한 Retrofit 인터페이스
 *//*

interface WikiApiService {
    @GET("w/api.php")
    suspend fun getHistoricalEvents(
        @Query("action") action: String = "query",
        @Query("format") format: String = "json",
        @Query("prop") prop: String = "extracts",
        @Query("titles") titles: String, // 요청할 연대 이름
        @Query("exintro") exintro: Int = 1,
        @Query("explaintext") explaintext: Int = 1
    ): WikiResponse
}*/
